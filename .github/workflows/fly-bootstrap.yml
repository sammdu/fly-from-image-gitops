name: Fly Bootstrap

on:
    workflow_dispatch:
    workflow_call:

jobs:
    bootstrap:
        uses: ./.github/workflows/fly.common.yml
        with:
            bootstrap: true
        secrets: inherit
